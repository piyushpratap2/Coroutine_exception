package com.example.coroutineexception

import android.annotation.SuppressLint
import kotlinx.coroutines.coroutineScope
import kotlinx.coroutines.launch

@SuppressLint("SuspiciousIndentation")
suspend fun main(args: Array<String>){
   /* val obj =  Singleton
    var result = obj.data(5,5)
    println("$result")
    obj.data2(15,10)
    var name:String = "Hello"
    var result1 = name+obj.data3(50).toString()
    println("$result1")*/
   /* obj.SetName("piyush")
    for(i:Int in 1..100){
      val job =   coroutineScope {
            launch { obj.counter() }
            launch { obj.counter() }
            launch { obj.counter() }

        }
        job.join()
    }
    println(obj.getCounter())*/
    /*println(obj.getCounter())
    for(i:Int in 1..80){
        coroutineScope {

        }
    }
    println(obj.getCounter())

    for(i:Int in 1..50){
        coroutineScope {
            launch { obj.counter() }
        }
    }*/




}
